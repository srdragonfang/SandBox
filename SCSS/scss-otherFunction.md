Scss still offers other functions that might be useful to you


## str-length($string)
> This function returns the number of characters in a string.

## percentage($number)
> This function converts number without unit to a percentage.

## und($number)
> This function rounds a number to the nearest whole number.

## min($number1, $number2, $number3, …..)
> this function returns the minimum value from a set of numbers.

## random()
> This returns a random number and takes no arguments.

## quote($string)
> This function adds quotes to a string.

## unquote($string)
> This function removes quotes from a string.

## to-lower-case($string)
> This function converts a string to lowercase.

## to-upper-case($string)
> This function converts a string to uppercase.